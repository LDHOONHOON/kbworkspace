function Person(name, gender, age, address, job) {
  this.name = name;
  this.gender = gender;
  this.age = age;
  this.address = address;
  this.job = job;
}

var person = new Person(
  'yura',
  'f',
  '15',
  'sky',
  'angel'
);

console.log(
  `이름 = ${human.name}의 age = ${human.age}`
);
